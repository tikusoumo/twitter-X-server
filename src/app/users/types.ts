export const types = /*graphql*/ `
type User {
    id: ID!
    firstName: String!
    lastNmae: String
    email: String!
    profileImageURL: String
    createdAt: String!
    updatedAt: String!
}
    `